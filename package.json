{
	"name": "test-ui-lib",
	"version": "0.1.0",
	"scripts": {
		"postinstall": "npm run bootstrap",
		"bootstrap": "lerna bootstrap",
		"build": "lerna exec --scope ui-lib npm run build",
		"start": "lerna exec --scope use-lib npm start"
	},
	"devDependencies": {
		"lerna": "^2.11.0"
	}
}
